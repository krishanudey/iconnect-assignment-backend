# iconnect-assignment-backend

After cloning, make a copy of `.env.example` and rename it as `.env`. Add your `MONGO_URI` to `.env` file

To run **dev**, execute `npm run dev`.  
To run **production**, execute `npm start`.
